# TODO

## 账号管理

- [ ] 账号查询接口
  - [ ] 列出账号池中全部账号（邮箱、用户名、登录状态、最近使用时间、使用次数）
  - [ ] 按状态过滤（有效 / 登录失败 / 被禁用）
- [ ] 账号增删改
  - [ ] 新增账号入库（手动录入邮箱 + 密码，惰性登录）
  - [ ] 删除 / 停用某个账号
  - [ ] 修改账号密码后重登
- [ ] 账号池增强
  - [ ] 账号健康检查：定期校验 remix_userid / remix_userkey 是否仍有效（如对 / 请求 200 非重定向）
  - [ ] 自动剔除失效账号并重新登录或移出池
  - [ ] 下载失败（Daily limit / 502 / 账号被风控）时自动轮换下一个可用账号
  - [ ] 并发下载时按账号并发度分配，避免同一账号同时过多请求被风控
- [ ] FFI 导出账号管理函数（如 `rlibrary_account_list` / `rlibrary_account_add` / `rlibrary_account_remove`）
- [ ] 账号池状态统计（总数 / 在线 / 失败数 / 今日下载次数）

## 注册接口补充

- [ ] 调研 z-library 注册流程（/registration 页面表单字段、密码规则、人机验证要求）
- [ ] 实现 `signup` API（邮箱 + 密码注册）
- [ ] 邮箱验证码 / 验证链接的处理（如存在）
- [ ] 注册成功后自动加入账号池
- [ ] FFI 导出 `rlibrary_signup`

## 其它

- [ ] 下载真正落地：目前 `resolve_download_url` 只返回 CDN 直链不下载文件（避免 SNI 作用于 CDN 域）。若需下载，应走独立 HTTP 客户端直连 CDN
- [ ] 204（`book_hash3_id` 下载门）时重试仍失败给出更精确错误文案
- [ ] `selectedSiteMode` 模式选择（books / articles / ...）可通过参数配置
